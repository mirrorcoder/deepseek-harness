# The panel manages destinations, so the row only needs to exist. Bots added
# from the page live in settings.yaml under `telegram:`; their tokens live in
# the credential store. A deployment wired through deploy/.env before the panel
# existed keeps working as a read-only destination.
- insert:
    - id: ext-telegram
      name: dsh-ext-telegram
      config:
        panel: true
